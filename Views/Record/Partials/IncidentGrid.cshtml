@model dynamic

<div class="table-responsive">
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Incidencia</th>
                @for(int day = ((int)Model.InitialDate); day <= ((int)Model.EndDate); day++ ){
                    <th class="text-center">@day</th>
                }
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
            <tr>
                <td>FugaTomaDomiciliaria</td>
                @for(int day = ((int)Model.InitialDate); day <= ( (int)Model.EndDate); day++ ){
                    <td class="text-center">0</td>
                }
            </tr>
        </tbody>
    </table>
</div>